# 学习版 notebook 核验记录

- 范围：默认四节点固定功率因数、无损有功平衡、线性平方电压模型；未进行 AC 复核。
- 从空内核运行 50 个单元中的全部 26 个代码单元，保存真实计算输出；单元无错误或警告。
- MP1：固定倍率 1.3，最低费用 32360 元，13 次主问题求解、12 条割。
- MP2：预算 33000 元，最大倍率 1.566887426743，11 次主问题求解、10 条割。
- 交替流程：7 轮、14 次查询、51 次内层主问题求解、37 条共享割。
- 完整前沿：独立枚举 216 个设计得到 7 个台阶，与 Benders + 自适应预算递推逐项一致；最大倍率差 2.22e-16。
- 电气核验：216 个设计各进行一次极限 LP 和一次超限 LP，共 432 次；并检查对偶可行性与强对偶。
- 查询核验：保留原项目的 15 个查询；追加 20 个预算跳点查询、6 个平台中点查询、21 个倍率边界查询。
- 轨迹核验：LB 不降、有限 UB 不升、接受时目标界闭合、割数增量一致、加割轮生成点被排除、不可行终止轮没有伪造 SP 结果。
- 费用粒度：默认费用系数最大公约数为 20 元；非整数费用和负费用会被递推入口拒绝。负预算可正确记录主问题直接不可行。
- 所有教学割在全部设计的可行倍率区间端点有效，最差余量 -1.94e-16，属于浮点误差量级。
- 绘图核验：逐张检查 10 幅图；修正数学符号缺字、单轮查询的刻度与浮点残差过度放大。费用和倍率分轴显示；无穷上界不伪造为有限曲线。
- 粗扫描对照：每 10,000 元采样一次，漏掉 51,020 元至 56,300 元之间的平台；扫描仅用于教学对照和割池快照。
- 代码图：修改前检查求解器调用影响；修改后执行 GitNexus detect_changes（完整结果，无 partial/truncated）。包含输出的 notebook 超过图索引默认大小限制，运行验证覆盖其全部代码单元。

主要输出：`mp1_trace.csv`、`mp2_trace.csv`、`alternating_all_iterations.csv`、`alternating_rounds.csv`、`frontier_crosscheck.csv`、`jump_boundary_checks.csv`、`lambda_boundary_checks.csv` 与 `figures/`。

所有记录来自本次真实求解，未使用构造的收敛轨迹。迭代路径/割数可能随 LP 基础、求解器版本和数值容差改变；前沿坐标通过独立校验确认。
