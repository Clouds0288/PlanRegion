# 节点功率图件的证据与检查

图1展示0—140 kW的最低预算前沿。图2在同一物理范围内比较20,000、30,000、40,000元和无建设预算上限，回答预算如何扩大节点分配可行域。
采用紧凑a/b/c/d分面、克制蓝/灰/橙配色、统一坐标与符号。静态科研图由Python/matplotlib绘制，用户要求的手动旋转与回放由Plotly离线交互图实现，两者共用实际多面体外表面。

零基准、固定功率因数、无损有功平衡、线性平方电压降，物理有功上限142.5 kW。无预算上限不删除电气约束。图件没有观测数据、统计重复或置信区间。

全部216个结构合法方案均参与认证。四个预算分别纳入17、53、105、216个方案；预算增大时方案集合和其负荷域并集嵌套。
绘图删除被单个其他多面体完全包含的几何成员，再逐面扣除内部覆盖；不对不同建设方案取整体凸包。
平面舍入只用于分组，几何遮挡判定保留原始平面精度。交互网格在每个实际边界面内约束三角化，近重合角点按1e-8 kW显示精度整理。原始求解多面体不受显示处理影响。

完整性来自最终1708个顶点的SP证书；原点有解析证书。候选顶点只取自当前割外近似，独立物理真值不参与选点或产生割。
204条割在216个设计的整个真实功率域上通过独立检验。总共1663轮，其中可行认证轮次不新增割。
相同割数复用同一网格；全部轮次的建设、SP运行变量、53行乘子、残差及所有割仍保留。

最终四个并集体积用独立容斥法计算，分别为41523.94956、107687.20982、152299.63458、360927.15119 kW³。
与交互三角网格围成的体积比较，相对误差依次为8.06e-11、3.94e-11、3.23e-11、1.57e-11。
最大总负荷依次为100、100、135、142.5 kW；20,000和30,000元的相同总量最优值不代表相同的三维域。

静态图源预检17项通过、0项失败；PDF实际最小字号6.3 pt，SVG保留文本，PDF嵌入TrueType字体，PNG为300 dpi。
预检的TIFF缺失/600 dpi建议不适用于本次以Notebook交互与矢量为主的交付；数学小字提示已由实际PDF字形审计复核。

数据可追溯至complete/process.json、replay.json、history.csv、cuts.csv、前沿CSV及verification.json。
Notebook与独立HTML共用同一回放界面；可切换本次进度/完整实验、按轮次/新增割播放、联动旋转，且切换轮次保持视角。
