# 配电网规划—可调度域

四节点五走廊算例：节点 0 为电源，节点 1、2、3 为负荷节点；三个节点负荷非负、自由分配。
不设置分区基准、区内映射或增长比例。建设方案逐一确定可调度域，预算内各方案取并集。

## 文件

- `Network/`：网架、设备、费用、固定背景负荷与方案生成；各网架输出统一的 `RadialNetwork`。
- `model.py`：MP1、MP2、规划线性 SP，以及网架通用的 `BranchEquations`、`LinearSP`、`SOCPSP`。
- `vertify.py`：网架通用的独立 AC、四方法比较、FR/MR 及唯一结果文件。
- `planning.py`：原线性单独求解和协同切割流程，供两个 Notebook 共用。
- `region.py`：候选域、对偶割、同方案认证、并集扣除、局部凸块与选点队列。
- `plot.py`：Benders 图、费用前沿、三维并集表面及交互绘图；只读取求解状态。
- `replay.py`：从历史重建回放，打包多个预算，控制预算演变与逐轮播放。
- `NodePower.ipynb`：四个代码块，运行线性单独求解和协同求解流程。
- `vertify.ipynb`：统一验证入口；修改 `CASE` 在 `case33bw` 与 `four_bus_five_corridor` 之间切换。
- `results/four_bus_five_corridor.html`：自包含离线回放，汇集四档预算，播放不调用求解器。

## 规划域定义

节点负荷向量的顺序为 `network.load_nodes`，当前为 `(1, 2, 3)`。
每个节点均满足 $p_i\ge0$；允许 `(100, 0, 0)` 等极端分布，由电气 SP 判断能否实现。

对固定建设方案 $x$，令 $\mathcal F_x$ 为存在可行潮流、电压的节点负荷集合。
预算 $B$ 下的规划—可调度域为：

$$\mathcal D(B)=\bigcup_{x:\,c^Tx\le B}\mathcal F_x.$$

**不同建设方案之间只取并集，不把所有方案的可行点放入一个整体凸包。**
固定方案下采用线性电气模型，因而可用同方案 SP 可行点的凸包认证一个内域：

$$\mathcal I_x=\operatorname{conv}\{p:\text{方案 }x\text{ 下已通过 SP}\},\qquad
\bigcup_x\mathcal I_x\subseteq\mathcal D(B).$$

蓝色显示认证内域的非凸并集。迭代中可能遗漏尚未认证的可行部分；完整认证后得到该模型的完整规划域。
每个蓝色点至少存在一种预算内方案，不要求同一个方案实现整个蓝色区域。

## 四个代码块

1. **载入。** 导入网架和模块；重跑此块重置实验。更改网架后重启内核并重新载入。
2. **MP1SP。** 修改 `POWER`，求指定节点负荷的最低建设费用，输出 Benders 过程图。
3. **MP2SP。** 修改 `BUDGET`，求预算内最大总负荷，输出 Benders 过程图。
4. **协同求解。** `BUDGETS=(20000., 40000., 60000., np.inf)`，分别认证四个预算下的完整域，并导出一份回放。

`STEPS=1` 每次推进一轮，`STEPS=20` 每次最多推进20轮；`PAUSE=1` 两轮之间暂停1秒。
最后一块的 `STEPS` 对每档预算分别生效；演示使用10000轮上限、无暂停，完成后自动停止。
重跑相同代码块会续算。**达到轮数上限仅暂停，不代表完整域已经认证。**

```python
cost_query = MP1SP(np.array([100., 0., 0.]))
cost_query.run(100, 0)
cost_query.result

load_query = MP2SP(40000.)
load_query.run(100, 0)
load_query.result
```

单独求解的 `result['power']` 就是节点负荷，不重复保存一份同值的 `node_power`。
`studies[20000.]` 等对象保留各档预算的求解状态；`cooperative` 指向预算列表的最后一档。

## MP1、MP2 与 SP

MP1 最小化建设费 $c^Tx$。默认固定节点负荷向量；`total=True` 只要求 $\sum_i p_i\ge P$，允许自由分配。
因此，总量 MP1 返回的是至少一种分配达到指定总量所需的最低费用，不保证所有分配都能以该费用实现。

MP2 在 $c^Tx\le B$ 下最大化 $\sum_i p_i$；无限预算仅取消建设费用约束。
两类主问题均包含节点负荷非负、配变容量、树结构和累计割。
树结构用单商品虚拟流保证连通，并选择 $n-1$ 条边。

SP 固定 $(x,p)$ 后求：

$$\min_{w,\eta\ge0}\eta,\qquad Ww-r\eta\le h+Tx+Dp.$$

当 $\eta>10^{-8}$ 时，由 $\pi=-\mathrm{Pi}$ 添加可行性割：

$$\pi^T(h+Tx+Dp)=\beta_0+\beta_x^Tx+\beta_p^Tp\ge0.$$

SP 采用固定功率因数0.95、无损有功平衡和线性平方电压降。
当前配变为150 kVA，有功上限142.5 kW；结论针对这一线性模型。

## 完整切割过程与停止规则

每档预算独立沿用同一流程：MP2SP → 总量 MP1SP → 预算降低一个费用粒度（当前20元），恢复费用前沿。
已收敛 MP2 的最大总负荷同时收紧相应预算内方案的候选域。
随后搜索各方案候选域中尚未被任何方案认证的部分：

$$\mathcal R_x=\mathcal U_x\setminus\bigcup_y\mathcal I_y.$$

1. 对候选域先作单个认证凸域的快速包含判断；否则逐个扣除认证域，实现真正的并集覆盖判断。
2. 扣除产生的局部凸块放入优先队列，按维数、体积（退化时面积或长度）、最大总负荷排序。
   未更新条目的大小是剩余贡献的上界；只有排到前面时，才应用新割、新认证域并重新排队。
3. 选中一块后先检查未覆盖顶点，再检查与已认证域相交产生的边界顶点，块内按总负荷降序。
   已被其他方案认证的交界点仍可用于证明本块在当前方案下整体可行；不会把跨方案顶点直接连成凸包。
4. 固定 $(x,p)$ 后直接调用原 SP。可行点加入同方案认证凸包，继续完成当前块；
   不可行时将参数化对偶割应用到各方案外域，当前块收缩后重新排队。
5. 队列耗尽且当前块已获证时自动停止：每块都已被有效割删除或被认证并集覆盖。
   不以体积很小、没有新割、所有顶点已被不同方案分别覆盖作为停止条件。

块内连续认证避免每新增一个可行点就重新分割，减少碎片和重复检查。
交点只在去重时使用舍入键，实际坐标保留原始精度，避免破坏共面性。
`region.GEOMETRY_TOL=1e-7` 为单位法向量下的覆盖距离容差，单位 kW；完整认证指该容差内的结果。

两个阶段都遵循“本轮求解 → 应用割/总量上界 → 同方案认证 → 选择下一查询”的顺序。
外域切割和内域认证从前沿阶段就交错发生。MP1SP、MP2SP 和 SP 的数学模型保持原样；
仅在顶点阶段省去变量均已固定的 MP1 重建。绘图不参与选点与停止判断。

初始候选域为 $p\ge0,\ \sum_i p_i\le142.5$ 的单纯形。
SP 割保留建设变量，对不同方案分别切割；不能因某方案失败而从所有方案中删除同一个负荷点。
只有同一方案的可行点能组成认证凸域，各方案之间始终只取并集。

`model.py` 定义模型，`planning.py` 控制原线性迭代，Notebook 设置实验，`region.py` 更新域；`replay.py` 从同一份历史调用域函数重建过程，不重新调用求解器。
MP1/MP2/SP 随非根节点数定义负荷变量；当前三维几何要求三个负荷节点，完整方案枚举仍用于小网架。

## 预算演变与切割回放

默认选择“预算演变”，依次展示 **20,000 → 40,000 → 60,000 → 无限预算** 的最终认证进度。
若求解完成，就是各档的完整域；未完成的实验会明确显示尚在认证。

选择某档预算后，可以切换到“完整切割过程”或“仅新增割”，从第0轮播放该预算的历史。
支持播放/暂停、首末位置、前后步进、进度拖动及指定轮次。
预算切换保持三维坐标尺度和旋转视角；总负荷—最低预算图也采用共同坐标尺度。

- 图1：所选预算上限内的费用前沿，灰线为候选能力上界，橙点已经 SP 认证。
- 图2：蓝色为各方案认证域并集；候选外域与前一候选域默认隐藏，可勾选观察切割。
- 每档 `history` 只记录一次原始查询结果；`queries` 保存查询参数。
- 网架参数在四档回放中只打包一次。内外域网格和时间轴从历史推导，网格只在变化时缓存。
- Notebook 只引用同一份 HTML，不重复嵌入回放数据。

`plot_regions(studies[20000.])` 绘制该预算的认证域；添加 `outer=True` 可对照候选域。
`plot_frontier(studies[20000.])` 绘制该预算的费用前沿。

## 本次完整运行与算法对比

| 预算（元） | 最大总负荷（kW） | 原方法 SP 次数 | 当前方法 SP 次数 | 当前割数 |
|---|---:|---:|---:|---:|
| 20,000 | 100.0 | 114 | 38 | 9 |
| 40,000 | 135.0 | 401 | 107 | 35 |
| 60,000 | 142.5 | 1,044 | 114 | 34 |
| 无限 | 142.5 | 1,106 | 60 | 28 |

四档合计从2,665次 SP 降为319次。当前方法为“按需扣除认证并集＋按贡献排序＋整块同方案认证”。
计时涵盖方案枚举、主问题、SP、全部域更新和选点，排除许可证首次连接、绘图及 HTML 导出。

各预算均串行重复三次，以下耗时取中位数（本机实测，不含绘图及导出）。

| 预算（元） | 原方法（秒） | 当前方法（秒） | 加速倍数 |
|---|---:|---:|---:|
| 20,000 | 0.756 | 0.219 | 3.4× |
| 40,000 | 5.452 | 0.908 | 6.0× |
| 60,000 | 31.119 | 1.454 | 21.4× |
| 无限 | 34.913 | 1.230 | 28.4× |

中位耗时合计 72.24 → 3.81 秒，约 19.0 倍加速，SP 次数减少 88.0%。

对照实验还比较了以下实现（候选方法为单次筛选计时）：

| 方法 | 四档 SP 合计 | 四档耗时（秒） | 结论 |
|---|---:|---:|---|
| 原选点，固定变量时直接调用 SP | 2665 | 62.92 | 未采用 |
| 按块认证，交点仍作坐标舍入 | 1038 | 18.55 | 未采用 |
| 按块认证，保留原值、沿多面体边切割 | 324 | 4.98 | 未采用 |
| 按块认证，保留原值、顶点对求交 | 319 | 3.83 | 采用，并对主线复测三次 |

逐点重新分割版本在20,000元、40,000元下分别需要43次／0.36秒、298次／11.58秒；
60,000元和无限预算均在15秒测试限时内未完成，未采用。没有将测试限时当作算法收敛。
这些比较支持当前小算例中的选择，不表示该方法对所有网架和预算均最快。

独立核验通过径向潮流消元构造全部216个合法方案的真实线性可行域，
逐一检查各预算下的认证点、参数化割以及候选外域；对不能由单个认证凸域覆盖的情形，
额外求解混合整数反例搜索，检验整个候选域是否存在认证并集外的点（包括内部空缺）。
本次233次反例搜索均未找到未覆盖点，费用前沿与原结果一致。

Notebook 四个代码块已重新执行；回放的319条记录和6,418个逐帧蓝色三角面均已核验。
另已验证内部空缺反例、交界顶点补全、多个认证域共同覆盖、退化域、薄剩余区域，以及单步／分批续算。

四档均已完成完整域认证。60,000元与无限预算的最大总负荷相同，但可行域不同：
节点负荷 `(0, 75, 65)` kW 的最低建设费为 **62,900元**，只属于后者。
`(100, 0, 0)` kW 的最低费用为14,300元；`(45, 5, 45)` kW 需要51,800元。

## 独立 AC 区域验证

`vertify.py` 定义完整非线性 AC 模型、统一四方法实验和网格比较。
`plot.py` 共用一个绘图入口；早期两方法实验作为历史记录保留在 `results/vertify/`。
当前四方法实验统一保存为 `results/<网架名>/result.npz`，由 `vertify.ipynb` 读取或重新计算。
记录区域不一致率，以及 Chen & Zhao (2023) 第5594页式(11)、(12)定义的 FR 和 MR：

$$E(B)=\frac{\operatorname{Vol}(\widehat{\mathcal D}_{\rm lin}(B)\triangle\mathcal D_{\rm AC}(B))}
{\operatorname{Vol}(\widehat{\mathcal D}_{\rm lin}(B)\cup\mathcal D_{\rm AC}(B))}\times100\%.$$

$$\mathrm{FR}=\frac{\operatorname{Vol}(\mathcal D_{\rm lin}\setminus\mathcal D_{\rm AC})}
{\operatorname{Vol}(\mathcal D_{\rm lin})}\times100\%,\qquad
\mathrm{MR}=\frac{\operatorname{Vol}(\mathcal D_{\rm AC}\setminus\mathcal D_{\rm lin})}
{\operatorname{Vol}(\mathcal D_{\rm AC})}\times100\%.$$

FR 衡量计算域中多余的比例，MR 衡量 AC 域中遗漏的比例。两者的分母不同；
本例在同一均匀网格上按点数计算，对应体积比例。空分母记为未定义，不记作零。

AC 参考独立枚举全部216个合法径向建设方案，保留线路损耗、完整电压降和电流—功率等式。
沿用原始线路 kW 上限，配变约束实际送端视在功率不超过150 kVA。
在初始负荷单纯形内逐点判断：存在一个预算内 AC 可行方案即属于 AC 规划域。
利用本算例正负荷、正阻抗条件下的单调电流迭代批量求解，未确定点交给显式非凸 QCQP；
求解失败不能记作不可行。线性结果读取已有回放中完成认证的各方案凸域并集。

红色表示 AC 可行但线性域遗漏的部分；黄色表示线性域多出的 AC 不可行部分；蓝灰色表示重合。
`region_comparison.html` 可离线打开，支持四图同步旋转、图层切换；不对非凸并集取整体凸包。
早期 `summary.json` 和 `grid_*.npz` 是历史输出；当前实现不再重复导出指标或网架参数。

| 预算（元） | 区域不一致率 | FR | MR |
|---|---:|---:|---:|
| 20,000 | 9.14% | 9.14% | 0.00% |
| 40,000 | 12.35% | 12.35% | 0.00% |
| 60,000 | 12.76% | 12.76% | 0.00% |
| 无限 | 12.83% | 12.83% | 0.00% |

结果采用0.625 kW网格中心分类，是有限分辨率的 AC 数值参考域。
相较1.25 kW网格，四档指标最大变化约0.104个百分点，不代表严格误差上界。
本次采样没有检出红色遗漏部分，差异来自黄色多余部分；不据此宣称连续域严格包含。
因此本次 FR 恰好等于区域不一致率，MR 为零。后续模型改进应在同一参考域上同时比较 FR 和 MR，
避免仅靠缩小计算域降低 FR、却显著增加 MR。
测试另以显式非凸模型逐方案交叉核验，并由复数电压和电流重建节点功率核对 AC 可行解。

## SOCP 四方法比较

打开 `vertify.ipynb`，设置 `CASE="four_bus_five_corridor"` 可读取本次归档实验；设置 `RECOMPUTE=True` 用当前代码重新求解并计时。
纯 SOCP 从单纯形开始；混合方法重新执行原 LP，再以其各方案候选外域初始化 SOCP。
两者使用同一径向停止精度 `τ=0.002`，主表比较切割外域，认证内域另存。独立 AC 不参与切割和停止判断。

四档预算中，FR 从纯线性的 9.14%–12.83% 降到两种 SOCP 的 0.017%–0.041%；
同一 0.625 kW 网格上 MR 均为零。纯 SOCP 四档总计 89.52 秒，混合方法 86.98 秒，
本次仅减少约 2.8%，尚不能宣称稳定的大幅加速。时间包括建模、求解、构域和网格判定，混合方法计入 LP 阶段。

- [完整 FR/MR、计时和验证结果](docs/socp_benchmark_results.md)
- [SOCP 数学模型与实现口径](docs/socp_model.md)
- [可旋转四方法对比图](results/four_bus_five_corridor/region_comparison.html)
- `results/four_bus_five_corridor/result.npz`：分类、计时、配置和去重后的几何记录，唯一原始结果。
- FR/MR、总时间、差集标签和 HTML 从该结果派生；归档的原计时与来源信息不因存储迁移而改写。

AC 是有限网格的完整非线性数值参考；网格 FR=0 或 MR=0 不等于连续边界完全一致。
两种 SOCP 的数学停止条件另给出连续内外域相对体积间隙上界 0.5988%，仅在当前模型假设下关联 AC 域。

## 33 节点固定方案实验

打开同一个 `vertify.ipynb`，设置 `CASE="case33bw"` 查看第一步结果。采用原始 MATPOWER case33bw，固定网架和设备，
仅节点 18、25、33 的负荷独立变化，其余 P/Q 固定；这是可调度域的三维截面，尚无规划决策。
电压限值为原始 0.9–1.1 p.u.，不人为添加算例中未给定的线路热限或配变容量。

独立 AC 基准最低电压为 0.91309 p.u.，有功损耗 202.68 kW，与 pandapower 节点潮流一致。
最终 192³ 网格上，纯线性 FR 为 34.47973%，纯 SOCP 为 0.03298%，线性＋SOCP 为 0.03277%，MR 均为零。
总计算时间依次为 0.397、1.786、1.836 秒，独立 AC 扫描为 52.786 秒；混合方法计入线性阶段，本次未体现速度优势。

- [实验范围与计划](docs/case33_plan.md)
- [结果、模型说明和验证记录](docs/case33_results.md)
- [33 节点可旋转三维对比](results/case33bw/region_comparison.html)
- `Network/case33bw.py` 只提供网架数据；与四节点共用 `model.py`、`vertify.py` 和 `plot.py`，不保留独立的 33 节点模型文件。

两种网架统一使用 `MethodBenchmark(network, budgets, divisions, radial_tolerance)`。
每次实验的 AC 掩码只存一次，FR/MR 和总时间按需计算；相同几何在不同预算出现时，文件仅保存一份并使用引用。
粗网格作为独立实验保存在 `grid_64/result.npz`、`grid_128/result.npz`，不再另写重复汇总表。
重构验证包含 15 项测试、216 个建设方案、24,832 个 AC 判定以及 33 节点 64³ 四方法逐点回归。

## 环境

需要 `requirements.txt` 中的依赖及有效 Gurobi 许可证。本次实测使用 `methods` 环境和同名 Notebook 内核，
SOCP 使用 Clarabel，线性模型及 AC 非凸兜底使用 Gurobi。也可自行建立项目环境：

```powershell
python -m venv .venv
.venv\Scripts\python -m pip install -r requirements.txt
.venv\Scripts\python -m ipykernel install --user --name planregion --display-name "Python (PlanRegion)"
```
